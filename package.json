{
  "homepage": "http://elkayyy.github.io/ReviewsProject",
  "name": "zoover-assessment",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:server": "lerna run start --scope server --stream",
    "start:client": "lerna run start --scope client --stream",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "react": "react-scripts build",
    "start": "lerna run start --parallel --scope server --scope client --stream"
  },
  "dependencies": {
    "build": "^0.1.4",
    "primeicons": "^6.0.1",
    "primereact": "^9.3.1"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "lerna": "^4.0.0"
  }
}
